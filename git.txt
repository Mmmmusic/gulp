git init
git status
git reflog

git add .
git commit -m " "
git status

git remote add origin https://github.com/Mmmmusic/gulp.git
git push -u origin master:master
git push origin master:master
git pull origin master:master

git clone https://github.com/Mmmmusic/gulp.git

先拉再传

用户名和邮箱 是安装完git的第一步操作